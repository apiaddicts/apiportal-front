
const initialState = {
    name: 'Headline Ad',
}

export const demoReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}